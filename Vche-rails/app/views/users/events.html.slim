h1 = User.model_name.human

= render 'strip', user: @user

h2 = EventFollow.model_name.human

= render 'events/cards', events: @events, edit: false, card_cloud: true
= paginate @events

= link_to 'New', new_event_path, class: 'button'
