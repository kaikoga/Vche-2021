h1 = "#{Account.model_name.human}を編集する"

= render 'form', user: @user, account: @account

.panel
  = link_to '戻る', @user, class: 'button'

- set_meta_title_for(@account.display_name, "#{Account.model_name.human}を編集する")
